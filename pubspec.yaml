name: isekai_guild_masters_calculator
description: Help to calculate score of isekai guild masters.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.5
  isar: ^3.0.5
  isar_flutter_libs: ^3.0.5
  logger: ^1.1.0
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  isar_generator: ^3.0.5
  build_runner: ^2.3.2

flutter:
  uses-material-design: true
  assets:
    - env/
